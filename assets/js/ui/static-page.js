/* Запуск переводов на страницах без симулятора — 404 и политика.
   Отдельным файлом, а не инлайном: CSP запрещает inline-скрипты,
   и это правильно, иначе главная защита от XSS выключается ради одной строки. */
TAKT.i18n.start();
